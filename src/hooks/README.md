# Custom Hooks

This directory contains custom React hooks used throughout the application.

## Planned Hooks

- `useAuth`: Authentication-related hooks
- `useRSVP`: Hooks for handling RSVP functionality
- `useGallery`: Hooks for photo gallery operations
- `useEvents`: Hooks for managing event data
- `useForm`: Form handling and validation hooks 